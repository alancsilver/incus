config:
  cloud-init.user-data: |
    #cloud-config
    packages:
      - podman
      - nmap
      - lsof
    users:
      - name: alan
        gecos: Alan Silver
        sudo: "ALL=(ALL) NOPASSWD:ALL"
  security.nesting: "true"
description: For podman
devices:
  kvm:
    path: /dev/kvm
    type: unix-char
name: podman
used_by: []
project: default
